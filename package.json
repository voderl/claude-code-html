{
  "name": "claude-code-share",
  "version": "0.1.0",
  "description": "Capture a Claude Code session via tmux and render it as a responsive, snapshot-toggleable HTML page.",
  "bin": {
    "claude-code-share": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "tsc",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "ansi_up": "^6.0.2",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
